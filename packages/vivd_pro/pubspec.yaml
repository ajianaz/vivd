name: vivd_pro
description: Vivd Pro — Server-backed face liveness with ML PAD, compliance, and face management.
version: 0.1.0
publish_to: pub.dev

environment:
  sdk: "^3.6.0"
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter
  vivd: ^0.1.0
  http: ^1.2.0
  crypto: ^3.0.3
  jwt_decode: ^0.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.0
