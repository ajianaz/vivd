name: vivd_advanced_example
description: Advanced example app for Vivd face liveness SDK — full config, callbacks, face identity.
publish_to: none

version: 1.0.0+1

environment:
  sdk: "^3.6.0"
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter
  vivd:
    path: ../../packages/vivd

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
